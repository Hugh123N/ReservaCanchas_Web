const defaultLanguage = 'es';
const languageMapping: Map<string, string> = new Map<string, string>();

languageMapping.set('en', 'en-US');
languageMapping.set('es', 'es-ES');

export { defaultLanguage, languageMapping };
