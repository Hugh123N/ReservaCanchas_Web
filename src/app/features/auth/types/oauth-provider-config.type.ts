/**
 * Configuración visual para proveedores OAuth.
 * Usado para renderizar botones y UI de cada proveedor.
 */
export interface OAuthProviderConfig {
  name: string;
  icon: string;
  color: string;
  bgColor: string;
  textColor: string;
}
