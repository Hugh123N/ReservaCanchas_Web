// Tipos (interfaces)
export type { OAuthStrategy } from './oauth-strategy.interface';

// Clases (valores)
export { GoogleOAuthStrategy } from './google-oauth.strategy';
export { FacebookOAuthStrategy } from './facebook-oauth.strategy';
